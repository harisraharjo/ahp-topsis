// import { ComponentPropsWithoutRef, ElementType, forwardRef, PropsWithChildren, ReactNode } from "react"
// import { Container } from "./heading.styles"

// type Props = PropsWithChildren<{
//   level: "1" | "2" | "3" | "4" | "5" | "6"
// }>
// type Variant = `h${Props["level"]}`

// export const Heading = <T extends ElementType<HTMLHeadingElement>>({
//   children,
//   level = "3",
//   ...rest
// }: Props & Omit<ComponentPropsWithoutRef<T>, keyof Props>) => {
//   const Component: Variant = `h${level}`

//   return <Component {...rest}>{children}</Component>
// }

export {}
